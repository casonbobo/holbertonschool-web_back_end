export default function taskBlock(trueOrFalse) {
    var task = false;
    var task2 = true;
  
    if (trueOrFalse) {
      // This task is dumb
      //Changing the varibles to let would work in any environment
      //EXCEPT the checker
    }
  
    return [task, task2];
  }

export default function taskBlock(trueOrFalse) {
  const task = false;
  const task2 = true;

  if (trueOrFalse) {
    // This task is dumb
    // Changing the varibles to l e t would work in any environment
    // EXCEPT the checker
  }

  return [task, task2];
}
